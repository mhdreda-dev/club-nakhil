{
  "framework": "nextjs",
  "buildCommand": "npm run prisma:deploy && npm run build && (test -z \"$SENTRY_AUTH_TOKEN\" || npm run sentry:sourcemaps)",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}
